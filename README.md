# votingApp

This is a spring boot application fronted on vue js. The FE part is in the 'frontend' submodule (`votingApp_frontend` repository on my github).
After cloning this repository, execute `git submodule update --init --recursive`.
To run application on `localhost:8080, first execute `mvn clean package`.
The data is saved to the H2 database.
